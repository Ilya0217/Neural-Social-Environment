# Скопируйте в inventory.ini и при необходимости поправьте.
# inventory.ini в .gitignore — это локальный файл, не коммитится.

[prod]
agent-dialogue ansible_host=176.108.249.8 ansible_user=agent

[prod:vars]
ansible_python_interpreter = /usr/bin/python3
